# MLT Esports Platform — план работы и прогресс

Живой документ. Обновляется по мере работы — отмечаю `[x]` то, что сделано,
`[ ]` то, что ещё нет. Не удалять между сессиями — по нему продолжаем работу.

БД: **Firebase Firestore** (+ Firebase Auth для логина).
**Firebase Storage не используется** — на новых проектах он требует платный
план Blaze, поэтому проект сделан на стандартном бесплатном пакете **Spark**:
- лого команды — пользователь выбирает файл, он сжимается на клиенте до
  маленькой картинки и сохраняется как base64 прямо в документе команды
  в Firestore (без отдельного бакета);
- обложка турнира / фото новости — админ вставляет обычную ссылку (URL)
  на картинку, размещённую где угодно (imgur, свой хостинг и т.п.).

---

## 0. Стартовая точка

- [x] Есть готовый статический шаблон (`mlt-template__1_.html`) — вся вёрстка,
      все страницы, все стили сделаны. Данные захардкожены в JS-массивах
      (TEAMS/PLAYERS/MATCHES/NEWS/TOURNAMENTS/BRACKETS).
- [x] Логотип MLT получен (файл `photo_2026-07-16_15-17-46.jpg`).

---

## 1. Firestore-схема (утверждено, менять только всем документом сразу)

```
users/{uid}
  email, nickname, role: "user" | "admin",
  teamId: string|null           // ссылка на свою команду (если создана)
  createdAt

teams/{teamId}
  name, logoUrl, ownerUid,          // logoUrl — base64 data URL (без Storage), не HTTP-ссылка
  main: [ {nick, realName?}, ... ]     // 5 основных
  reserve: [ {nick, realName?}, ... ]  // до 2 запасных
  rating, winrate, trophies, form: ["W","L",...] (последние 5)
  mapWinrate: { [mapName]: {wins, losses} }
  createdAt

players/{playerId}                     // профиль игрока = запись состава команды
  nick, teamId, weapon,
  kd, adr, kast, rating              // агрегаты, пересчитываются после матча
  achievements: [string]
  formGraph: [number]                // рейтинг за последние матчи
  matchIds: [matchId]                 // для «матчи игрока»

tournaments/{tournamentId}
  name, period, prizePool, status: "upcoming"|"live"|"done",
  coverUrl,                           // обложка турнира — обычная внешняя ссылка (URL), без Storage
  regOpen: boolean,
  registeredTeamIds: [teamId],
  bracket: {...}                      // структура сетки (раунды/матчи)
  winnerTeamId

matches/{matchId}
  tournamentId, teamA, teamB, format: "BO1"|"BO3"|"BO5",
  startAt (timestamp), status: "upcoming"|"live"|"finished",
  streamUrl: string|null              // ссылка на трансляцию — НОВОЕ
  score: [a,b] | null,
  maps: [ {name, score, winnerTeamId,
           playerStats: { [playerId]: {k,d,a,dmg,adr,kast,rating} } } ]

news/{newsId}
  title, excerpt, body, tag, imageUrl,   // imageUrl — обычная внешняя ссылка (URL), без Storage
  publishedAt

teamRegistrations/{regId}             // заявки команд на турнир
  tournamentId, teamId, contactInfo, status: "pending"|"approved"|"rejected",
  createdAt

messages/{id}                         // как на InfinitySite — обращения формы обратной связи (если нужно)
```

Автоматика (клиент/облачная функция при подтверждении результата матча):
- пересчёт `players.*` (K/D, ADR, KAST, rating) из `maps[].playerStats`
- пересчёт `teams.form`, `teams.winrate`, `teams.mapWinrate`
- топ-5 игроков турнира = агрегация `playerStats` по всем матчам с этим `tournamentId`, сортировка по `rating`, автоматически, без ручного ввода

Таймер → Live: Cloud Functions с расписанием нужны план Blaze (платный).
На плане Spark (бесплатный) делаем проще: при открытии страницы матчей/турнира
клиент сам проверяет `startAt <= now && status === "upcoming"` и обновляет
статус на `"live"` в Firestore (у первого посетителя, кто зайдёт после старта).
Хватает для реального продакшена такого масштаба.

---

## 2. Фазы работы

### Фаза A — быстрые правки в статическом шаблоне (без бэкенда)
- [x] Обложка турнира — поле `coverUrl` + показ на странице турнира (ссылка на картинку задаётся в админке; без ссылки — заглушка-градиент)
- [x] Фото у новостей — поле `imageUrl` + показ в карточках (ссылка на картинку задаётся в админке; без ссылки — заглушка)
- [x] Ссылки на соцсети в футере (сейчас `href="#"` — нужно подставить реальные ссылки)
- [x] Все матчи турнира — показываются на странице турнира (не только сетка)
- [x] Топ-5 игроков турнира — блок на странице турнира, считается автоматически из `playerStats` матчей этого турнира
- [x] Матчи игрока — блок в профиле игрока, с личной статистикой по завершённым матчам
- [x] Автообновление статуса upcoming → live по таймеру (реализовано в
      Фазе B клиентским таймером, план Spark)

### Фаза B — Firebase-подключение
- [x] Проект Firebase подключён (конфиг получен, `projectId: mlt-site`)
- [x] Firebase Auth: регистрация/вход/выход (реальные, не заглушка)
- [x] Профиль (модалка): смена ника, смена пароля, выход
- [x] Создание команды (название, логотип → сжимается на клиенте и
      сохраняется как base64 в Firestore, без Storage, 5 основных + 2 запасных)
- [x] Правило: нет команды → модалка регистрации на турнир подсказывает
      сначала создать команду
- [x] Форма регистрации на турнир — использует реальную команду
      пользователя (не ручной ввод состава с нуля)
- [x] Админ-панель: заявки на регистрацию → принять/отклонить →
      автодобавление команды в `tournament.registeredTeamIds`
- [x] Админ-панель: поле «ссылка на трансляцию» у матча
- [x] Плеер трансляции на странице матча (YouTube/Twitch embed по ссылке)
- [x] Автоматический пересчёт статистики игроков (K/D, ADR, KAST, rating)
      и команд (winrate, форма, winrate по картам) после ввода результата
- [x] Firestore Security Rules — см. `firestore.rules`, вставить в консоль
      Firebase → Firestore → Rules. Firebase Storage не используется вообще
      (не нужен, `storage.rules` больше не актуален и не требуется),
      поэтому в консоли Firebase раздел Storage можно не подключать.
- [x] Автообновление статуса upcoming → live клиентским таймером (план Spark)
- [x] Деплой на Firebase Hosting — файлы подготовлены (`deploy/firebase.json`,
      `deploy/.firebaserc`, `deploy/public/index.html`), пошаговая
      инструкция в `deploy/DEPLOY.md`. Сам деплой выполняется вами локально
      (`firebase deploy`), т.к. это требует входа в ваш аккаунт Google —
      это последнее, что нельзя сделать за вас удалённо.
- [x] Полноценные модалки для админских форм «добавить команду / игрока /
      матч / турнир / новость» — заменили `prompt()` на настоящие формы с
      полями (текст/select/checkbox/файл), в едином стиле с остальными
      модалками сайта.
- [x] Внесение результата матча тоже переведено с цепочки `prompt()` на
      модалку: карты добавляются/убираются кнопками (до 5), у каждой карты
      поля счёта и построчная статистика игроков обеих команд
      (K/D/A/DMG/ADR/KAST/Rating) — пустая строка игрока просто не
      учитывается, как и раньше.
- [x] Редактор турнирной сетки (bracket) из админки — новая вкладка
      «Сетка турнира»: выбор турнира, произвольное число раундов, в каждом
      раунде произвольное число матчей (название команды — текстом, это
      осознанно: на поздних стадиях сетки команда ещё может быть
      неизвестна, "Победитель матча 3" и т.п. тоже валидный текст), счёт
      можно оставить пустым (матч ещё не сыгран). Сохраняется в
      `tournament.bracket`, рендерится на публичной странице турнира без
      изменений (формат данных тот же, что был раньше).

### Известные упрощения текущей версии (см. также низ `firestore.rules`)
- Первый когда-либо зарегистрированный пользователь автоматически становится
  админом (без Cloud Functions, т.к. они требуют платный план Blaze).
  **Зарегистрируйте себя как администратора сразу после первого запуска**,
  до того как ссылку на сайт увидит кто-то ещё.
- Данные в Firestore пока пустые (в проекте `mlt-site` ещё ничего нет).
  В Админ-панели есть кнопка «Импортировать демо-данные» — она доступна
  только админу и только один раз (если коллекция команд ещё пуста),
  наполняет базу тем же демо-контентом, что был захардкожен в шаблоне.
- Форма «ссылка на трансляцию» у матча — единственное место в админке,
  где остался простой `prompt()` (это один текстовый вопрос, отдельная
  модалка ради одного поля не даёт выигрыша); всё остальное — уже
  полноценные модалки.
- Редактор турнирной сетки (bracket) — это ручной ввод раундов/матчей
  админом (текстовые названия команд + счёт), без автоматического
  «продвижения» победителя в следующий раунд и без привязки к реальным
  teamId. Это осознанное упрощение: авто-прогрессия сетки — заметно
  более сложная логика (нужно однозначно решать, как определять
  победителя, как обрабатывать переигровки и т.п.), и добавлять её
  сейчас — это отдельная следующая итерация, если она понадобится.
- Storage не используется, поэтому: (1) лого команды хранится как сжатая
  base64-картинка прямо в документе Firestore — предел ~300 КБ на поле,
  что с большим запасом покрывает клиентское сжатие до ~5-15 КБ;
  (2) обложка турнира и фото новости — это просто внешняя ссылка (URL),
  которую вводит админ, а не загрузка файла. Если понадобится настоящая
  загрузка файлов для этих двух полей — это Storage и план Blaze, либо
  внешний сервис хранения картинок с загрузкой через их API.

---

## 3. Готово для использования

Файл `mlt-site.html` теперь самодостаточный — подключается напрямую к
Firebase (`mlt-site` проект) через CDN-модули, никакого сборщика не нужно.
Используется только стандартный бесплатный пакет **Spark**: Firestore + Auth.
Firebase Storage не задействован вообще — картинки не нужно ни включать,
ни настраивать в консоли. Отдан только `firestore.rules` — его нужно
вставить в консоль Firebase (Firestore → Rules) и нажать Publish, иначе по
умолчанию Firestore либо всё запрещает, либо (если оставили тестовый режим)
всё разрешает — оба варианта нежелательны. Файл `storage.rules` больше не
нужен, его можно не использовать.

Порядок первого запуска:
1. В консоли Firebase вставить `firestore.rules`, Publish. Раздел Storage
   трогать не нужно — сайт им не пользуется.
2. Открыть сайт, зарегистрироваться первым — этот аккаунт станет админом.
3. В Админ-панели нажать «Импортировать демо-данные» (по желанию) — или
   сразу начать вносить свои команды/игроков/матчи.
4. Дальше — обычная работа: создание команд пользователями, заявки на
   турниры, ввод результатов матчей админом (статистика пересчитывается
   автоматически).
5. Когда готовы опубликовать сайт — папка `deploy/` уже собрана
   (`firebase.json`, `.firebaserc`, `public/index.html`), инструкция в
   `deploy/DEPLOY.md`: `firebase login`, затем `firebase deploy --only hosting`
   из папки `deploy/`. Это единственный шаг, который нужно выполнить у себя
   локально — он требует входа в ваш Google-аккаунт.

## 4. Статус: MVP полностью готов

Все пункты Фазы A и Фазы B закрыты. Админка полностью на нормальных
модалках (не `prompt()`, кроме одного текстового поля «ссылка на
трансляцию»), есть рабочий редактор турнирной сетки, весь бэкенд —
на бесплатном плане Firebase Spark, без Storage. Из осознанных
упрощений остались только те, что описаны в разделе 2 выше
(бутстрап первого админа, ручная сетка без авто-прогрессии) — это
уже не «недоделки», а сознательные архитектурные решения для MVP
такого масштаба; расширять их стоит только если реально понадобится.

---

## 5. Фаза C — Расширение функциональности (дополнительно)

### 5.1. Улучшения UX и навигации
- [x] **Сохранение языка** — выбор языка (RU/EN) сохраняется в localStorage,
      при обновлении страницы язык не сбрасывается
- [x] **Сохранение активной вкладки** — текущая страница и параметры навигации
      сохраняются в localStorage, при обновлении страницы пользователь
      остаётся на той же вкладке
- [x] **Страница "Контакты"** — новая вкладка с контактной информацией
      (Email, Telegram, Discord) и формой обратной связи, сообщения
      сохраняются в коллекцию `messages` Firestore

### 5.2. Расширение админ-панели
- [x] **Редактирование новостей** — добавлена кнопка редактирования (✎) в таблице
      новостей админ-панели, при клике открывается модалка с заполненными
      полями для редактирования существующей новости
- [x] **Редактирование турниров** — добавлена кнопка редактирования (✎) в таблице
      турниров админ-панели, при клике открывается модалка с заполненными
      полями для редактирования существующего турнира

### 5.3. Профили игроков
- [x] **Расширенное редактирование профиля** — добавлена отдельная модалка для
      редактирования профиля зарегистрированного игрока:
      - Фото (сжатие на клиенте, сохранение как base64 в документе пользователя)
      - Никнейм
      - Email
      - Telegram
      - Discord
      - Elo рейтинг
      - Faceit профиль (ссылка)
- [x] **Кнопка редактирования** — в модалке профиля добавлена кнопка
      "Редактировать профиль игрока" для открытия расширенной формы редактирования

### 5.4. Конфигурационный файл
- [x] **config.js** — создан отдельный конфигурационный файл для редактирования
      текстового контента сайта без изменения кода:
      - Контактная информация (Email, Telegram, Discord)
      - Ссылки на социальные сети
      - Тексты навигации и кнопок
      - Тексты пустых состояний
      - Заголовки и описания разделов

### 5.5. Стратегия работы на бесплатном плане Firebase Spark
Для продолжения работы после истечения бесплатного периода:

**Текущая архитектура уже оптимизирована для бесплатного плана:**
- **Firebase Storage не используется** — все изображения хранятся как base64
  в документах Firestore (лого команд, фото пользователей) или как внешние
  ссылки (обложки турниров, фото новостей)
- **Cloud Functions не используются** — вся логика на клиенте, автоматическое
  обновление статуса upcoming → live выполняется клиентским таймером
- **Firestore Security Rules** — настроены для безопасной работы без серверных функций

**Лимиты Firebase Spark (на момент написания):**
- Firestore: 50K чтений/день, 20K записей/день
- Хранилище: 1GB (не используется)
- Бандл: 5GB/месяц (Firebase Hosting)
- Аутентификация: безлимитная

**Рекомендации для продления работы на бесплатном плане:**
1. **Оптимизация чтений Firestore** — кэширование данных на клиенте,
   использование `onSnapshot` только для критических обновлений
2. **Оптимизация изображений** — агрессивное сжатие на клиенте (уже реализовано),
   использование внешних хостингов для больших изображений
3. **Мониторинг использования** — регулярная проверка консоли Firebase для
   отслеживания приближения к лимитам
4. **Архивирование старых данных** — удаление или архивирование старых
   матчей/новостей для снижения количества документов

**При необходимости масштабирования:**
- Переход на план Blaze (pay-as-you-go) при превышении лимитов
- Рассмотрение альтернативных бесплатных решений (Supabase, Appwrite)
- Оптимизация архитектуры для снижения количества операций Firestore

---

## 6. Статус: Фаза C завершена

Все пункты Фазы C выполнены. Сайт теперь имеет:
- Постоянное сохранение языка и навигации
- Страницу контактов с формой обратной связи
- Расширенную админ-панель с редактированием новостей и турниров
- Полноценное редактирование профилей игроков
- Отдельный конфигурационный файл для простого изменения текстов

Архитектура остаётся полностью совместимой с бесплатным планом Firebase Spark.

---

## 7. Фаза D — Реструктуризация в мультистраничный сайт

### 7.1. Новая структура проекта
Сайт перестроен из одностраничного приложения (SPA) в мультистраничный сайт,
аналогичный структуре InfinitySite 3.0. Каждая вкладка теперь открывается как
отдельная HTML страница с органичной навигацией между страницами.

**Новая структура файлов:**
```
d:/Заказ/MLT/111/
├── index.html                    # Главная страница
├── matches.html                  # Матчи
├── rankings.html                 # Рейтинги
├── tournaments.html              # Турниры
├── news.html                     # Новости
├── contacts.html                 # Контакты
├── admin.html                    # Админ-панель
├── index-old.html                # Резервная копия оригинального SPA
├── config.js                     # Конфигурационный файл
├── assets/
│   ├── css/
│   │   └── style.css            # Общие стили (вынесены из HTML)
│   └── js/
│       ├── firebase-config.js    # Конфигурация Firebase
│       └── main.js               # Общая логика JavaScript
└── PROJECT_PLAN.md
```

### 7.2. Выполненные изменения
- [x] **CSS вынесен в отдельный файл** — `assets/css/style.css` содержит все стили
- [x] **JavaScript вынесен в отдельный файл** — `assets/js/main.js` содержит общую логику
- [x] **Firebase конфигурация вынесена** — `assets/js/firebase-config.js` для удобства
- [x] **Созданы отдельные HTML страницы** для каждого раздела:
  - `index.html` — Главная
  - `matches.html` — Матчи (с вкладками: Ожидаются, В эфире, Результаты)
  - `rankings.html` — Рейтинги (с вкладками: Команды, Игроки)
  - `tournaments.html` — Турниры (с вкладками: Идут/скоро, Завершены)
  - `news.html` — Новости
  - `contacts.html` — Контакты
  - `admin.html` — Админ-панель
- [x] **Общий header/footer** — каждая страница имеет идентичный header и footer
- [x] **Навигация обновлена** — ссылки в header/footer указывают на реальные HTML файлы
- [x] **Модалка авторизации** — добавлена на все страницы
- [x] **Админ модалки** — добавлены в admin.html для всех CRUD операций

### 7.3. Особенности новой архитектуры
- Каждая страница загружается независимо, как обычный HTML файл
- Общие стили и JavaScript подключаются через `<link>` и `<script>` теги
- Firebase инициализируется один раз при загрузке каждой страницы
- Данные загружаются через Firestore `onSnapshot` для реального времени
- Язык сохраняется в localStorage и применяется на каждой странице
- Навигация между страницами происходит через обычные ссылки (не JavaScript)

### 7.4. Преимущества мультистраничной структуры
- **SEO-оптимизация** — каждая страница имеет свой URL и может быть проиндексирована
- **Быстрая загрузка** — загружается только нужная страница, не весь SPA
- **Простота поддержки** — каждая страница — отдельный файл, легче вносить изменения
- **Органичность навигации** — пользователи привыкают к обычным ссылкам между страницами
- **Совместимость** — работает без JavaScript (базовая навигация)

### 7.5. Статус: Фаза D завершена

Реструктуризация в мультистраничный сайт полностью завершена. Сайт теперь
соответствует структуре InfinitySite 3.0 с отдельными HTML файлами для каждого
раздела, общими стилями и JavaScript, органичной навигцией между страницами.

---

## 8. Фаза E — Интеграция доработанного admin-функционала + починка страниц-деталей

Загруженный отдельно `main.js` (1400+ строк) содержал полноценную реализацию
админ-панели (CRUD для команд/игроков/матчей/турниров/новостей, заявки на
регистрацию, редактор турнирной сетки, внесение результата матча с
автопересчётом статистики) — то, чего не было в `assets/js/main.js` из
`site.zip` (там админка была лишь заглушкой `// Admin panel rendering would
go here`). Эта версия внедрена как основной `assets/js/main.js` сайта.

### 8.1. Обнаруженные и исправленные проблемы при интеграции
- [x] **`admin.html` не хватало id для модалок** — заголовки модалок и кнопки
      сохранения (`atModalTitle`/`atSubmitBtn`, `apModalTitle`/`apSubmitBtn`,
      `amModalTitle`/`amSubmitBtn`, `attModalTitle`/`attSubmitBtn`,
      `anModalTitle`/`anSubmitBtn`), а также кнопки «+ Добавить…»
      (`btnAddTeam`, `btnAddPlayer`, `btnAddMatch`, `btnAddTournament`,
      `btnAddNews`) — без них открытие модалок и переключение
      «Добавить/Редактировать» не работало. Добавлены все недостающие id.
- [x] **`match-details.html`, `player-profile.html`, `team-profile.html` были
      полностью нерабочими** — подключали старый **compat** Firebase SDK
      (`firebase-app-compat.js` и т.п.) и вызывали `main.js` как обычный
      `<script>`, хотя `main.js` — ES-модуль (`import ... from
      firebase-config.js`) и использует **модульный** SDK v10. Кроме того,
      их инлайновые скрипты обращались к глобальным `TEAMS`, `tagBlock()`
      и т.п., которых не существует вне модуля. Итог: эти 3 страницы не
      грузили данные и падали в консоли с ошибками. Исправлено: у всех трёх
      страниц скрипты заменены на стандартные
      `<script type="module" src="assets/js/firebase-config.js">` +
      `<script type="module" src="assets/js/main.js">`, а логика рендера
      перенесена внутрь `main.js` (функции `renderMatchDetailsPage`,
      `renderPlayerProfilePage`, `renderTeamProfilePage`) и подключена через
      `getCurrentPage()`/`renderPageContent()` — как и остальные страницы
      сайта, с реактивным обновлением через `onSnapshot`.
- [x] **Вёрстка страниц-деталей переписана под уже существующие CSS-классы**
      (`.md-teams-panel`, `.md-vs`, `.stat-table`, `.profile-head`,
      `.stat-cards`, `.two-col`, `.panel-box`, `.map-wr-row` и т.д. —
      они уже были в `style.css`, но не использовались, т.к. сломанные
      инлайн-скрипты рендерили другую, нигде не описанную разметку).
      Теперь все 3 страницы визуально согласованы с остальным сайтом.
- [x] **Поле «ссылка на трансляцию» матча** — было упомянуто в плане как
      единственный `prompt()` в админке, но отсутствовало в загруженной
      версии `main.js`. Восстановлено: кнопка «Трансляция» в таблице матчей
      админки → `prompt()` → сохраняет `streamUrl` в Firestore.
- [x] **Не было способа выйти из аккаунта** — кнопка входа для
      залогиненного пользователя вызывала `alert()` без выхода, а
      `profileModal`, на который ссылался `handleLogout()`, нигде не
      существует. Временное исправление: клик по кнопке пользователя
      показывает `confirm()` и разлогинивает через `signOut()`.

### 8.2. Известные пробелы (не в рамках этой сессии, требуют отдельного решения)
- **Регистрация пользователем команды и заявка на турнир** — согласно
  `Задание.txt` это ключевая функция («Регистрация» на странице турнира,
  форма создания команды), но ни в одном html multi-page файле нет модалки
  создания команды/регистрации на турнир (была в старой SPA-версии,
  потерялась при переходе на мультистраничность в Фазе D).
- **Расширенное редактирование профиля пользователя** (фото, никнейм,
  Email, Telegram, Discord, Elo, Faceit — было сделано в Фазе C) —
  тоже отсутствует в мультистраничной версии; сейчас у пользователя вообще
  нет своего профиля/модалки, кроме входа и разлогина через `confirm()`.
- **`config.js`** существует и заполнен текстами, но ни одна HTML-страница
  его не подключает и `main.js` его не читает — сейчас это мёртвый файл.
  Чтобы он заработал, нужно либо подключить `<script src="config.js">` на
  каждой странице и переписать хардкод-тексты на чтение из
  `window.MLT_CONFIG`, либо конфиг убрать, если он больше не нужен.
- Эти три пункта — по сути откат функциональности Фаз B/C при
  реструктуризации в Фазу D. Стоит решить, куда двигаться: либо
  восстанавливать (создание команды + заявка на турнир + профиль
  пользователя) поверх текущей мультистраничной структуры, либо явно
  сознательно исключить их из MVP.

### 8.3. Статус
Админ-панель теперь полноценно работает на всех формах (создание,
редактирование, удаление команд/игроков/матчей/турниров/новостей, приём/
отклонение заявок, редактор сетки, внесение результата с автопересчётом
статистики). Публичные страницы деталей матча/игрока/команды тоже теперь
рабочие и стилистически согласованы с сайтом.

---

## 9. Фаза F — Закрытие пробелов из раздела 8.2

- [x] **Модалка «Профиль»** (`profileModal`, на всех страницах) — открывается
      по клику на никнейм/email в шапке вместо старого `confirm()`+`signOut()`.
      Позволяет сменить никнейм, показывает свою команду (если создана) со
      ссылкой на её профиль, кнопка «Создать команду» (если её ещё нет),
      кнопка «Выйти из аккаунта». Заодно устранён мёртвый код в
      `handleLogout()`, который ссылался на несуществовавший `profileModal`
      (теперь модалка реально существует и функция рабочая).
- [x] **Модалка «Создать команду»** (`createTeamModal`) — название, логотип
      (сжимается на клиенте, base64, без Storage — как и раньше у админа),
      5 полей основного состава (обязательны), до 2 полей запасных
      (опционально). Сохраняет `teams/{id}` с `ownerUid = текущий uid` и
      проставляет `users/{uid}.teamId`.
- [x] **Модалка «Заявка на турнир»** (`tournamentRegModal`) — кнопка
      «Регистрация» на карточке турнира (`tournaments.html`) теперь реально
      работает: не вошёл → открывает `authModal`; вошёл, но нет команды →
      открывает «Создать команду» и после её создания автоматически
      продолжает подачу заявки на тот же турнир; есть команда → форма
      контакта → `addDoc` в `teamRegistrations` (с проверкой на повторную
      заявку той же команды на тот же турнир).
- [x] **`config.js` подключён** (`<script src="config.js">` на всех
      страницах) и используется: ссылки соцсетей в футере (`data-social`
      атрибуты + `applyConfig()` в `main.js`) теперь реально берутся из
      `MLT_CONFIG.social`, а не остаются как `href="#"`. Остальные текстовые
      поля конфига (кнопки, пустые состояния, тексты навигации) пока не
      подключены — сейчас это тексты «зашиты» напрямую в HTML/JS; чтобы
      использовать их тоже, нужно заменить хардкод-строки на чтение из
      `window.MLT_CONFIG` по каждому месту отдельно — можно сделать по мере
      необходимости.

### Известные пробелы, оставшиеся сознательно вне этой сессии
- Расширенный профиль (фото, Email, Telegram/Discord, Elo, Faceit) —
  сделана только базовая версия (никнейм + своя команда + логаут);
  остальные поля из Фазы C пока не восстановлены поверх мультистраничной
  структуры.
- Правки состава команды (добавить/убрать игрока после создания) —
  сейчас состав задаётся только один раз при создании команды; изменить
  его после может только админ через админ-панель (редактирование
  игроков там уже есть).

---

## 10. Фаза G — Elo/Faceit + контакты, редактирование состава, i18n (RU/EN)

Ограничение этой сессии: всё сделано **строго в пределах Firebase (Spark,
бесплатный план) и GitHub** — без Cloud Functions/Blaze и без сторонних
прокси-хостингов (Cloudflare/Vercel/Netlify).

### 10.1. Elo/Faceit + контакты в профиле
- [x] Новые поля в `users/{uid}`: `telegram`, `discord`, `faceitNickname`,
      `faceitUrl`, `faceitElo`, `faceitLevel`, `faceitUpdatedAt`,
      служебные `faceitUpdateRequested`, `faceitUpdateRequestedAt`,
      `faceitUpdateError`.
- [x] UI в `profileModal`: блок «Контакты» (Telegram/Discord/ник Faceit +
      сохранение), блок «Faceit» (текущие Elo/уровень, когда обновлялись,
      кнопка «Обновить Elo с Faceit»).
- [x] **Архитектурное решение по ключу Faceit** (см. `FACEIT_SETUP.md`):
      без Cloud Functions единственный вариант спрятать ключ — отдельный
      серверный процесс. Выбран **GitHub Actions по расписанию** (каждые
      ~20 минут) + `workflow_dispatch` для ручного запуска:
      `.github/workflows/faceit-elo-sync.yml` запускает
      `scripts/faceit-sync.mjs`, который через Firebase Admin SDK (секрет
      `FIREBASE_SERVICE_ACCOUNT`) находит пользователей с
      `faceitUpdateRequested == true`, дёргает Faceit Data API v4 (секрет
      `FACEIT_API_KEY`, оба секрета — только в настройках GitHub-репозитория,
      не в коде сайта) и пишет `faceitElo/faceitLevel/faceitUrl/
      faceitUpdatedAt` обратно, сбрасывая флаг. 404 и 429 от Faceit
      обрабатываются отдельно и не роняют весь прогон.
      **Компромисс:** обновление не мгновенное (до ~20 минут), зато
      полностью бесплатно и без привязки карты — см. `FACEIT_SETUP.md` для
      пошаговой настройки секретов.
- [x] `firestore.rules`: перепроверено — существующее правило `users/{uid}`
      уже разрешает владельцу документа писать любые свои поля кроме
      `role`, отдельного правила под новые поля не потребовалось; добавлен
      только поясняющий комментарий.

### 10.2. Редактирование состава команды после создания
- [x] Новая модалка `editTeamModal` (разметка как у `createTeamModal`, но
      предзаполняется текущими данными команды) на всех страницах.
- [x] Кнопка «Редактировать состав» в блоке «Моя команда» в `profileModal`
      (видна только владельцу команды) → `openEditTeamModal(teamId)` →
      `updateDoc(doc(db,"teams",teamId), {...})`. Позволяет менять название,
      логотип, 5 основных и до 2 запасных. Rules уже разрешали `update`
      владельцу команды — правок не потребовалось.

### 10.3. i18n (RU/EN)
- [x] Единый словарь `assets/js/i18n.js` (183 ключа, RU/EN 1:1) + `t(key)`
      в `main.js`.
- [x] `applyStaticI18n()` — проходит по `[data-i18n]` / `[data-i18n-placeholder]`
      / `[data-i18n-title]` в статическом HTML, вызывается и при переключении
      языка, и при первой загрузке страницы (учитывает `localStorage`).
- [x] Переведено: шапка/нав, футер, hero и секции главной, страницы
      Матчи/Рейтинги/Турниры/Новости/Контакты (заголовки, вкладки, пустые
      состояния, лейблы форм и плейсхолдеры), все модалки, включая новые
      (`profileModal` целиком, `createTeamModal`/`editTeamModal`,
      `tournamentRegModal`, `authModal`), страницы деталей
      (`match-details.html`, `player-profile.html`, `team-profile.html` —
      кнопка «назад», заголовки, лейблы состава/статистики/формы), статусы
      матчей и турниров, сообщения об ошибках входа/регистрации.
- [x] Верхний уровень админ-панели (заголовок, вкладки слева, заголовки
      секций, кнопки «+ Добавить...», состояния загрузки) переведён через
      `data-i18n`.

**Сознательно не переведено (см. ниже, почему):** внутренние формы админки
(модалки добавления/редактирования команды/игрока/матча/турнира/новости,
редактор турнирной сетки, форма внесения результата) и функции
`renderAdmin*`/`openAdd*`/`openEdit*` в `main.js` остались на русском.
Причина: это внутренний инструмент самого администратора сайта (не
публичная часть), а не только экономия времени — большинство этих функций
используют локальную переменную `t` как сокращение для команды/турнира/
матча (`const t = teamById(id)`), которая конфликтует с функцией перевода
`t()`; чтобы не переименовывать переменные по всей админке с риском
что-то сломать, было решено ограничить перевод публичной частью сайта и
верхним уровнем оболочки админки. Если нужно — можно сделать отдельной
сессией с точечным рефакторингом этих функций.
